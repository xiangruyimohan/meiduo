from django.conf.urls import urlfrom . import viewsurlpatterns = [    # 去结算    url(r'^orders/settlement/$', views.OrderSettlementView.as_view()),    # 保存订单    url(r'^orders/$', views.CommitOrderView.as_view()),    # 获取订单数据    # axios.get(this.host+'/orders/'    url(r'^getorders/$', views.ListAllOrderGoodsView.as_view()),    # 进行订单商品的评论    url(r'orders/(?P<order_id>\d+)/uncommentgoods/', views.UncommentgoodsView.as_view()),    # 订单商品评论提交    # axios.post(this.host+'/orders/'+this.order_id+'/comments/    url(r'^orders/(?P<order_id>\d+)/comments/$',views.SavecommentView.as_view()),]